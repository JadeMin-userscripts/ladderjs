{
	"private": true,
	"type": "module",
	"name": "ladderjs",
	"version": "0.0.0",
	"scripts": {
		"esbuild": "node --no-warnings --experimental-vm-modules ./scripts/build.js",
		"build": "npm run esbuild"
	},
	"devDependencies": {
		"esbuild": "^0.17.4"
	}
}